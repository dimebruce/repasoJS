// Arrays

// Declarar un arreglo

const tech = [100, 20, 'Angular', 'React']

console.table(tech)


// Acceder a React dentro del arreglo, recordar que los arrays su índice empieza en 0

console.table(typeof tech[3])  //React

// Imprimir todo el array pero como String

console.table(tech.toString())
