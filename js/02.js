// Variable const

// No se puede reasignar

// No pueden inciar sin ningun valor

// Reglas

// Las variables nop pueden iniciar con números o carácteres especiales, sin embargo el guion bajo si está permitido. 

// Const tiene las siguientes caracteristicas

// - No se pueden reasignar
// - No se pueden declarar sin ningun valor

// Const es más utilizado que let, más que nada por la interacción con el DOM de HTML, pues este nunca cambia. 


const cliente = "Juan"
const precio  = 220
const precioDos = 10

console.log(cliente)
console.log(precio)
console.log(precioDos)